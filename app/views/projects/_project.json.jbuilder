json.extract! project, :id, :project_name, :user_stories, :wireframes, :erd, :proposal, :technologies, :features, :bonus, :issues, :created_at, :updated_at
json.url project_url(project, format: :json)
